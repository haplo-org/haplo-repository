
To create a demo application:

  db/init_app.sh haplo HOSTNAME "Haplo RM Repository" minimal APP_ID

Then upload plugins with:

  haplo-plugin -p hresrepodemo_application -s HOSTNAME

On app's home page, click "Generate test data", and choose Faculty/Department.


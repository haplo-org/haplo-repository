title: Datacite 
module_owner: Tom
--

This plugin provides all the functionality to export outputs compatible datacite schema,

h3(config). "hres:repository:datacite_should_not_export_contributors"

Configuration data to control the exporting of contributors. Set to true to prevent contributors being exported (Editors will always still be exported).